//! Starling Desktop — native GUI client.
//!
//! Uses the starling-server library for all protocol handling and
//! provides a native GUI (egui / iced / slint — TBD).

fn main() {
    println!("Starling Desktop — coming soon.");
}
